# Trigger Validation — vibe-eu-opc-mer-product-page

## SHOULD trigger (5)
1. "Viết product page cho leggings Husky AOP mới" → đúng (write product page).
2. "Page này convert thấp, cần viết lại copy + CRO" → đúng (ngữ cảnh page convert thấp).
3. "Làm trang bán hàng + upsell sports-bra cho SP EU này" → đúng (upsell bundle).
4. "Chèn nhãn GPSR vào PDP và thêm social proof" → đúng (GPSR label + social proof trên PDP).
5. "SP mới cần copy mô tả sản phẩm cho ShopBase" → đúng (SP mới cần copy).

## SHOULD NOT trigger (5) — bẫy
1. "Setup product, variant size, pricing và sync ShopBase cho SP này" → **vibe-eu-opc-mer-catalog** (catalog/pricing/sync, KHÔNG phải copy).
2. "Chạy FB Ads / scale campaign / tối ưu ROAS cho lô SP" → **vibe-eu-opc-grw-fb-ads** (ads).
3. "Viết video script / hook / carousel creative cho ad" → **vibe-eu-opc-grw-creative** (ad creative, KHÔNG phải product page copy).
4. "Thiết kế file AOP print-ready / mockup 300 DPI cho leggings" → **vibe-opc-pod-product-design** (design AOP).
5. "Làm clearance GPSR / IP-TM check / cấp Responsible Person" → **vibe-opc-pod-backoffice-compliance** (clearance, không phải chèn label vào page).

## Lưu ý ranh giới
- Skill này CHÈN nhãn GPSR và VERIFY clearance log ID, nhưng KHÔNG tự cấp clearance (đó là compliance).
- Skill này viết PDP copy, KHÔNG setup product/pricing/sync (đó là catalog).
