# Smoke test — mer-orchestrator
1. skill.json valid.
2. merch-plan --run-all → ok, evidence verified.
3. go_live item images_on_disk=false → FAIL (gate).
4. design_clearance enum khớp 01 prd (CLEAR/MODIFY/REJECT/PENDING).
