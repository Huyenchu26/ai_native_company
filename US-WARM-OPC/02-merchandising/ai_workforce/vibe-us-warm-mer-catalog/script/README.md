# script/ — Shared harness (no per-skill validator)
Dùng chung `../../../../_shared/script/validator.py` (fix bug EU: 1 validator = 1 nguồn sự thật).
Gọi: `python3 ../../../../_shared/script/validator.py --run-all --artifact <out.json> --schema schema/<x>.schema.json`
